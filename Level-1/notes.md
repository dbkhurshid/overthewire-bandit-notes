# Bandit Level 1

## What I learned

I used the `ls` command to find the file in the directory.

I then used the `cat` command to read the contents of the file and get the password for the next level.

## Commands I used

```bash
ls
cat <filename>
```

## Key takeaway

I learned how to use `ls` to find files and `cat` to read their contents.
